# Copy this file to android/keystore.properties and keep the copy out of git.
# storeFile is relative to the android/ directory.
storeFile=keystores/qst-upload.jks
storePassword=replace-with-upload-keystore-password
keyAlias=qst-upload
keyPassword=replace-with-upload-key-password
